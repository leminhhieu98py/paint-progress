# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.env.local
.env.test
.env.test.local

# Security scan output. Kept out of the repository on purpose: these reports
# enumerate the system's remaining weaknesses in the order an attacker would
# want them, and this repository is public.
vbsec-reports/

# Claude Design prototypes. Reference material for the UI work, not source --
# the screens are built from them, they are not built from the screens.
claude-design/
